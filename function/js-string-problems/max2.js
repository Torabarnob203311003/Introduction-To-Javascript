const dela = 25;
const gym = 35;
const chiku = 55;

if(dela > gym && dela > chiku) {
    console.log('dela will get the cake');
} else if(gym > dela && gym > chiku) {
    console.log('gym will get the cake');
} else {
    console.log('chiku will get the cake');
}