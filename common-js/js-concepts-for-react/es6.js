const student {

    name :' sakib al hasan',
    age: 32,
    movies : ['king jhan' , 'god is one']
}
//1.templet string
const about =` my name is ${student.name} age is ${student.age}`
//2. arrow function

 const getFifthyFive =() => 55;
 const addDicyFive = num=> number+65;
 const ISEven= X => X%2==0;
 const addThree = (x,y,z)=> x+y+z;
 const doMah = (num,num2)=>{


const sum = num1+num2;
    return sum ;

 }

 //speard operator

  const newNumbers = [...againnumbers];
  numbers.push(99)
  //create a new array from and older array and add an element
  const currentNumbers = [...numbers,55]