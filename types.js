//  var price = '31'
// //  console.log(typeof price);
//  var name = 65;
//  console.log(typeof name);
// //  var pass = true;
//  console.log (typeof pass);

        // so we know that by typesof we can know the types or value by terminal 


         var num1 = 0.2;
         var num2 = 0.1;
         var sum =num1+num2;
         sum = sum.toFixed(1);
         console.log(sum);

        //   so we know that bye to fixed we can make the real value of 0.1-0.2 